{
  "name": "server",
  "type": "module",
  "main": "api/index.mjs",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run api/index.mjs"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@rooms/api": "workspace:*",
    "@rooms/auth": "workspace:*",
    "@rooms/db": "workspace:*",
    "@rooms/env": "workspace:*",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@rooms/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "^5"
  }
}
